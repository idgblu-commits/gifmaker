# gifmaker

## Run

```powershell
python -m pip install -r requirements.txt
python app.py
```

The app opens a transparent screen overlay with a resizable selection rectangle.

Use the corner handles to resize the area and the center `+` handle to move it.
Press `Start` in the control panel to record only the selected rectangle, then `Stop` to save a GIF into the `recordings` folder.
